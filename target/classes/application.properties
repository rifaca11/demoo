spring.application.name=demo
# Configuration de la source de donn�es H2
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sharifa
spring.datasource.password=cires
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Console H2 (facultatif, pour acc�der � la console web H2)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Enable Swagger UI
springdoc.swagger-ui.enabled=true

# Path for Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html

# Path for API documentation (OpenAPI specification)
springdoc.api-docs.path=/api-docs


#changement du port
server.port=9090

# Afficher les requ�tes SQL dans la console
spring.jpa.show-sql=true

# Strat�gie de cr�ation de la base de donn�es
spring.jpa.hibernate.ddl-auto=update
